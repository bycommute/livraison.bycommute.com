# Copier en .env ou .env.local et remplir (fichier non versionné)
# Préfixe VITE_ pour exposition au client (phase 1 – dev local).
# En phase 2 (Netlify), la clé sera en variable d'env serveur et l'app passera par une Netlify Function.

VITE_ODOO_URL=https://votre-instance.odoo.com
VITE_ODOO_DATABASE=
VITE_ODOO_API_KEY=
